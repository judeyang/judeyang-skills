# Expected Timeline Notes

- Timeline order follows capture metadata where available.
- Manifest records fallback ordering decisions and unsupported files.
- Still images have explicit durations and frame-safe timing.
- Generated FCPXML has `fcpxml`, `resources`, `library`, `event`, `project`, `sequence`, and `spine` elements.
- Diagnostics should produce reduced files when Final Cut rejects the full import.
